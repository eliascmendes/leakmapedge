# Bancada virtual

Tudo que gera sinal. Nada aqui consome resultado de detector.

- `modelos`: gerador do arquivo de rede EPANET (.inp) a partir dos parametros
  do ensaio, para que mudar a posicao do vazamento nao exija edicao manual.
- `codigo`: scripts de simulacao transiente (TSNet, metodo das caracteristicas).
- `ambiente`: versoes travadas e notas de instalacao.
